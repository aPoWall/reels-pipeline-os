# Article Reference

Use this public artifact as:

> **Reels Pipeline OS** is an open visual map for editing short-form videos with an agent-assisted workflow. It describes the full path from raw recording to reusable editing state: source audit, transcript with word timings, source blocks, EDL, overlay lanes, cover lane, render route, QA and public-safe route sessions.

Short link:

https://apowall.github.io/reels-pipeline-os/

## One-Paragraph Description

The project turns Reels editing into a reusable protocol system with route sessions, branch packs, repeatable state and public-safe examples. A creator or agent can choose a source type, goal, editing mode and route, then generate P0-P7 protocols, expected artifacts, a JSON config and a route-specific prompt. Public examples use generic timings, labels, overlay lanes and cover mechanics so the method can be shared without exposing footage, transcripts, private names, faces or local project paths.

## Current Route Set

- `reel_edit` – private render spine with final MP4, text-free base and QA;
- `block_timeline` – inspectable timeline board with source blocks and overlay events;
- `palmier` – editable Palmier board with native text clips and winner cutlane;
- `shaper` – public proof/process overlay with restrained monochrome grammar;
- `cover_lab` – cover/visual-board pass with clean bases, separate text layer, contact sheet and style tokens;
- `article_research` – public research branch with sources, route matrix and article summary.

## Citation Notes

- Main artifact: `index.html`
- Editing protocols: `PROTOCOLS.md`
- Operating spec: `STACK.md`
- Contribution rules: `CONTRIBUTING.md`
- Example branch: `branches/portable-iphone-kit/`
- Public sources and community credits are listed on the `sources` tab.
- Research sessions and same-source test plan are listed on the `approaches` and `config` tabs.
- The current live configurator emits `reels.stack.config.v7`.
